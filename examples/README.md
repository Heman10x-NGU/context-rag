# Public examples

The files in this directory are synthetic, non-sensitive documents used to
show the expected input shape. They are intentionally small and are not a
benchmark corpus.

Point `RAG_VAULT_DIR` at `examples/notes` to build a tiny local index. Your
real notes, transcripts, and agent-memory files remain on your machine and
are ignored by Git.
